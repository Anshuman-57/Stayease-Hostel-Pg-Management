import React,{useEffect,useState}from'react';
export const Card=({title,value,children})=><div className="card">{title&&<h3>{title}</h3>}{value!==undefined&&<div className="metric">{value}</div>}{children}</div>;
export const Table=({columns=[],rows=[]})=><div className="tableWrap"><table><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={r._id||i}>{columns.map(c=><td key={c.key}>{c.render?c.render(r):String(r[c.key]??'-')}</td>)}</tr>)}</tbody></table></div>;
export function ModalForm({title,fields,onSubmit,button='Add',initial={}}){const[open,setOpen]=useState(false);const[form,setForm]=useState(initial);useEffect(()=>setForm(initial),[open]); const change=e=>setForm({...form,[e.target.name]:e.target.value}); return <><button onClick={()=>setOpen(true)}>{button}</button>{open&&<div className="overlay"><form className="modal" onSubmit={e=>{e.preventDefault();onSubmit(form).then?.(()=>setOpen(false));}}><h2>{title}</h2>{fields.map(f=><label key={f.name}>{f.label}<input name={f.name} type={f.type||'text'} value={form[f.name]||''} onChange={change} placeholder={f.placeholder||''}/></label>)}<div className="row"><button type="submit">Save</button><button type="button" className="secondary" onClick={()=>setOpen(false)}>Cancel</button></div></form></div>}</>}
